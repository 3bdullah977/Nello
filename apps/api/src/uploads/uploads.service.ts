import { Injectable } from '@nestjs/common';

@Injectable()
export class UploadsService {
  // async uploadFile(req: FastifyRequest, rep: FastifyReply) {
  //   const parts: AsyncGenerator<MultipartFile> = req.file;
  //
  //
  //   for await (const part of parts)
  // }
}
